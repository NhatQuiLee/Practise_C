#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>
#include <string.h>
#include "ForLoop_Workspace.h"
#include "WhileLoop_Workspace.h"
#include "ConditionalStatements_Workspace.h"
#include "Array_Workspace.h"
#include "Struct_Workspace.h"
#include "Pointer_Workspace.h"
#include "String_Workspace.h"
#include "Recursion_Workspace.h"


int main()
{
    
    
    // printf("Name: ");
    // fgets(student.name, sizeof(student.address), stdin);
    // student.name[strcspn(student.name, '\n')] = '\0';
    // printf("Address: ");
    // fgets(student.address, sizeof(student.address), stdin);
    // printf("Pincode: ");
    // scanf("%d", &student.pincode);

    
    // struct EmployeeInfor employee;
    // struct EmployeeInfor *ptr = &employee;
    
    // EnterEmployeeInfor(&employee);

    // DisplayEmployeeInfor(ptr);
    
    
    // int number;
    // int *array;
    // printf("Number of array: ");
    // scanf("%d", &number);
    // array = (int *)calloc(number, sizeof(int));
    // for (int i = 0; i < number ; i++)
    // {
    //     printf("Array[%d] = ", i);
    //     scanf("%d", array + i);
    // }
    // printf("Array = [");
    // for (int i = 0; i < number; i++)
    // {
    //     printf("%d ", *(array + i));        
    // }
    // printf("]");
    // int largest_number = *array;
    // for (int i = 1; i < number ; i++)
    // {
    //     if (*(array + i) > largest_number)
    //     {
    //         largest_number = *(array + i);
    //     }
    // }
    // printf("Largest number = %d", largest_number);
    
    // printf("Length of the string = %d", CalculateLengthOfString(str) - 1);


    // struct Car car1, car2, car3;
    // int rental_rate;

    // printf("Input detail for Car 1:\n");
    // InputCarInformation(&car1);

    // printf("Input detail for Car 2:\n");
    // InputCarInformation(&car2);

    // printf("Input detail for Car 3:\n");
    // InputCarInformation(&car3);

    // printf("Enter number of rental day: ");
    // scanf("%d", &rental_rate);

    // printf("\nRental rate Car 1: %0.2f", CalculateRentalRate(car1, rental_rate));
    // printf("\nRental rate Car 2: %0.2f", CalculateRentalRate(car2, rental_rate));
    // printf("\nRental rate Car 3: %0.2f", CalculateRentalRate(car3, rental_rate));
    
    return 0;
}